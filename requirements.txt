catboost==1.2.3
category-encoders==2.3.0
dython==0.5.1
icecream==2.1.2
libzero==0.0.8
optuna==2.10.1
rtdl==0.0.9
skorch==0.11.0
tomli-w==0.4.0
tomli==1.2.2
tqdm==4.62.3

# Compiled scientific stack.
# Keep the original pins for the paper's reference environment, but use
# wheel-friendly equivalents on Apple Silicon where older pandas/pyarrow
# source builds are brittle.
numpy==1.21.4; platform_system != "Darwin" or platform_machine != "arm64"
numpy==1.23.5; platform_system == "Darwin" and platform_machine == "arm64"
pandas==1.3.4; platform_system != "Darwin" or platform_machine != "arm64"
pandas==1.5.3; platform_system == "Darwin" and platform_machine == "arm64"
pyarrow==6.0.0; platform_system != "Darwin" or platform_machine != "arm64"
pyarrow==10.0.1; platform_system == "Darwin" and platform_machine == "arm64"
scikit-learn==1.0.2; platform_system != "Darwin" or platform_machine != "arm64"
scikit-learn==1.1.3; platform_system == "Darwin" and platform_machine == "arm64"
scipy==1.7.2; platform_system != "Darwin" or platform_machine != "arm64"
scipy==1.9.3; platform_system == "Darwin" and platform_machine == "arm64"

# smote
imbalanced-learn==0.7.0

# tvae
# The original TVAE baseline depends on rdt 0.6.x, which in turn requires
# scipy<1.8. That stack is difficult to install from pip on Apple Silicon,
# so we skip it there unless the environment is managed separately.
rdt==0.6.4; platform_system != "Darwin" or platform_machine != "arm64"
